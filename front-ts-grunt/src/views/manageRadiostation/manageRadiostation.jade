.center-area(data-editable='false')
  header.center-header
    .view-path Pregledaj podatke o postaji
  .center-content
    header.content-header
      .content-header-left
        .content-header-title Podaci o postaji
        .content-header-subtitle
          | Pregledajte i uređujte
          | vaše osobne korisničke podatke, koji nam koriste kako
          | bismo vam mogli pružiti što bolju uslugu slušanja i sudjelovanja
          | u radu postaje.
      .content-header-right
        i.material-icons.icon radio
    nav.content-options.uneditable-ui
      button( '*ngIf'="isOwner && isFormDisabled" '(click)'="toggleEditing()" ) Izmijeni podatke o postaji
    .content-primary.content-form
      fieldset('[disabled]'="isFormDisabled")
        form('[ngFormModel]'="myForm" '(ngSubmit)'="onSubmit(myForm.value)")
          .form-line
            label(for='name') Naziv postaje
            input(type='text', id='name', '[ngFormControl]'="myForm.controls['name']" )
          .form-line
            label(for='description') Kratki opis
            textarea(id='description' '[ngFormControl]'="myForm.controls['description']" )
          .form-line
            label(for='oib') OIB
            input(type='text', id='oib', '[ngFormControl]'="myForm.controls['oib']" )
          .form-line
            label(for='address') Adresa postaje
            input(type='text', id='address', '[ngFormControl]'="myForm.controls['address']" )
          .form-line
            label(for='email') E-mail adresa
            input(type='email', id='email', '[ngFormControl]'="myForm.controls['email']" )
          .form-line
            label(for='frequency') Frekvencija
            input(type='number', id='frequency', min='80', max='120', step='0.1', '[ngFormControl]'="myForm.controls['frequency']" )
          div( '*ngIf'="!myForm.valid" class="errorMessage" ) 
            | Neispravan unos
          .form-line.editable-ui
            .left-spacer
            button.raised.alt.right(type="button" '(click)'="resetRadiostation()" ) Odustani
            button.raised.left(type="submit" '[disabled]'="!myForm.valid") Spremi promjene
