.center-area( '[ngClass]' = '{"state-uneditable" : !editable, "state-editable" : editable }' )
  header.center-header
    .view-path Uredi zvučne zapise
  .center-content
    header.content-header
      .content-header-left
        .content-header-title Zvučni zapisi
        .content-header-subtitle
          | Pregledajte i uređujte
          | postojeće te dodajte nove zvučne zapise u sustav radiopostaje,
          | čime omogućujete urednicima da ih reproduciraju u programu postaje.
      .content-header-right
        i.material-icons.icon library_music
    nav.content-options.uneditable-ui
      button( '(click)' = 'toggleEditable()' ) Uredi zvučne zapise
    nav.content-options.editable-ui
      button( '[routerLink]' = '["/Settings", "AddTrack"]' ) Dodaj zvučni zapis
      button( '(click)' = 'toggleEditable()' ) Završi uređivanje zapisa
    ul.content-primary.content-list
      li.content-list-item.track-item( '*ngFor'="#track of tracks" )
        .content-list-item-content
          .content-list-item-name {{track.title}}
          .content-list-item-data
            .content-list-item-data-item.track-artist {{track.artist}}
            .content-list-item-data-item.track-album {{track.album}}
            .content-list-item-data-item.track-year {{track.year}}
            .content-list-item-data-item.track-genre {{track.genre}}
        .content-list-item-options.editable-ui
          button.dim
            i.material-icons( '(click)'="editTrack(track)") edit
          button.alt
            i.material-icons( '(click)'="deleteTrack(track)") clear
